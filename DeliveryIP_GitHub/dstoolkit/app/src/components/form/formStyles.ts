import { makeStyles } from "@fluentui/react-components";

export const formStyles = makeStyles({
    labelStyles: {
        fontWeight: "600",
        display: "inline-block",
        width: "20%",
        maxWidth: "150px",
        paddingTop: "6px",
        flexShrink: "0",
    },
    textAreaStyles: { height: "92px" },
});
